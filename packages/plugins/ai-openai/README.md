# ai-openai

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ai-openai` to build the library.

## Running unit tests

Run `nx test ai-openai` to execute the unit tests via [Jest](https://jestjs.io).
