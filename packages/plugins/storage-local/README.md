# storage-local

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build storage-local` to build the library.

## Running unit tests

Run `nx test storage-local` to execute the unit tests via [Jest](https://jestjs.io).
