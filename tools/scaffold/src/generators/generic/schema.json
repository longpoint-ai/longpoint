{
  "$schema": "https://json-schema.org/schema",
  "$id": "Generic",
  "title": "",
  "type": "object",
  "properties": {
    "scope": {
      "type": "string",
      "enum": ["api", "web", "shared"],
      "description": "The scope of the package.",
      "x-prompt": "What is the scope of the package?"
    },
    "name": {
      "type": "string",
      "description": "The singular name of the type.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the singular name of the type? (decorator, validation, etc.)"
    },
    "includeTests": {
      "type": "boolean",
      "description": "Whether to include tests.",
      "default": false,
      "x-prompt": "Do you want to include tests?"
    },
    "pluralName": {
      "type": "string",
      "description": "A plural name for the category. (decorators, validations, etc.)"
    }
  },
  "required": ["scope", "name"]
}
