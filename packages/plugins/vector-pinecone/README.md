# vector-pinecone

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build vector-pinecone` to build the library.

## Running unit tests

Run `nx test vector-pinecone` to execute the unit tests via [Jest](https://jestjs.io).
